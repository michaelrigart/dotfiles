#!/usr/bin/env bash

# make sure fnotify process gets killed afterwards
on_exit() {
    ssh weechat -O exit
    pkill -f mqtt-notify.py
}
trap on_exit INT TERM KILL EXIT

~/.config/bash/bin/mqtt-notify.py > /dev/null &
ssh -f -N weechat
ssh weechat -t .weechat/screen
