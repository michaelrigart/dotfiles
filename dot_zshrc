#!/usr/bin/env zsh

source "$HOME/.config/zsh/zshenv"
source "$HOME/.config/zsh/config"
source "$ZSH/oh-my-zsh.sh"
source "$HOMEBREW_PREFIX/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
source "$HOME/.config/zsh/aliases"
source "$HOME/.config/zsh/functions"

# Keybindings — must come after oh-my-zsh.sh, which resets the emacs keymap and
# drops any custom bind set earlier. Ghostty maps Cmd-k → Alt-k (its built-in
# clear_screen no-ops in the alternate screen / inside Herdr); Herdr deliberately
# binds no alt+k, so the chord reaches this binding rather than being consumed.
# At a bare shell prompt there is no multiplexer in the path at all, so the same
# two links (Ghostty sends the chord, zsh binds it) apply directly.
bindkey '\ek' clear-screen

# Added by LM Studio CLI (lms)
export PATH="$PATH:/Users/michael/.lmstudio/bin"
# End of LM Studio CLI section

